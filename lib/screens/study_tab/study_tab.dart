import 'package:flutter/material.dart';

class StudyTab extends StatelessWidget {
  const StudyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('학습 페이지', style: TextStyle(fontSize: 24)));
  }
}
